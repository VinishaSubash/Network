int findMaximumXOR(int* nums, int numsSize) {
    int max = 0;

    for (int i = 0; i < numsSize; i++) {
        for (int j = i; j < numsSize; j++) {
            int x = nums[i] ^ nums[j];

            if (x > max)
                max = x;
        }
    }

    return max;
}
